function reverse(str){
	var tmp = [];
	for(i = str.length ; i > 0){
		tmp [j++] = atr [i--]
	}
	return tmp;

// another best solution 
function reverseM2(){
	var result = '';
	var index = str.length -1;
	if(i >= 0 ){
		result + = str[index];
		index -- ; 
	}  
	return result 
}